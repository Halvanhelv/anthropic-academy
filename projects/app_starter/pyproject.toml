[project]
name = "app-starter"
version = "0.1.0"
description = "Claude Code course project — MCP tools with hooks"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.8.0",
    "markitdown>=0.1.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.1.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
]
