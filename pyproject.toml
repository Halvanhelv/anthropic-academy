[project]
name = "anthropic-academy"
version = "0.1.0"
description = "Anthropic Academy — Building with the Claude API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.51.0",
    "python-dotenv>=1.1.0",
    "voyageai>=0.3.0",
]
